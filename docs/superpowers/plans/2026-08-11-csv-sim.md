# CSV tekrar-oynatma simülatörü — uygulama planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `veri/` altındaki 262 Provis ölçüm CSV'sini döngüsel olarak yerel bir SQLite veritabanına yazan bir simülatör, `basla.bat`'a kendi modu ve yazılanı görünür kılan salt okunur bir teşhis ucu.

**Architecture:** Saf ayrıştırıcı (`csv_reader.py`) → yazıcı döngü (`replay.py`) → SQLAlchemy modelleri (`models.py`) → SQLite. Backend bu veriyi yalnız `GET /api/measurements` ile geri okur. Mevcut `hub`/SSE yolu ve frontend'e **hiç dokunulmaz**.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy 2.0, Alembic, SQLite (`pysqlite`, stdlib), pytest, uv.

**Tasarım:** `docs/superpowers/specs/2026-08-11-csv-sim-design.md`

## Global Constraints

- **Frontend'e tek satır dokunulmaz.** `frontend/` altında hiçbir dosya değişmez. `git status` bunun kanıtıdır (kabul ölçütü 6).
- **`domain.py` değişmez.** O dosya frontend `types.ts` ile birebir sözleşmedir. Yalnız `CamelModel` taban sınıfı oradan içe aktarılır.
- **`hub.py`, `live.py`, `adapters/`, `promos3/` değişmez.** Bu iş canlı UDP yoluna dokunmaz.
- Tanımlayıcılar İngilizce, yorumlar ve loglar Türkçe — `domain.py`'nin düzeni.
- Yorumlar **neden**i anlatır, ne'yi değil. Bu depoda her yorum bir hatadan öğrenilmiştir.
- Boş CSV hücresi `None` olur, **`0` olmaz**.
- Python komutları `backend/` dizininden `uv run ...` ile koşar.
- `basla.bat` **CRLF** satır sonlarıyla yazılır.
- Yeni bağımlılık **eklenmez**: `pysqlite` stdlib'dir.
- **Bilinen zemin:** `uv run pytest` bugün 60 test geçiyor — plan bitince hepsi hâlâ geçmeli. `uv run ruff check .` bugün 2 hata veriyor (`migrations/versions/0e4a34448ddb_baslangic.py` içinde kullanılmayan `op` ve `sa`); bunlar **bu işten önce vardır**, düzeltilmez ve sayı 2'nin üstüne çıkmaz.

## Dosya haritası

| Dosya | Sorumluluk | Görev |
|---|---|---|
| `veri/10660/*.csv`, `veri/10665/*.csv` | Ham ölçüm verisi (262 dosya) | 1 |
| `.gitignore` | DB dosyasını versiyonlamamak | 1 |
| `backend/app/config.py` | DB URL çözümü + sim ayarları | 2 |
| `backend/app/db.py` | Motor + SQLite PRAGMA'ları | 2 |
| `backend/tests/conftest.py` | Testleri geçici DB'ye sabitlemek | 2, 3 |
| `backend/tests/test_db.py` | URL çözümü ve PRAGMA'lar | 2 |
| `backend/app/models.py` | 3 tablo, tek sorumluluk: şema | 3 |
| `backend/migrations/env.py` | `target_metadata` bağlama | 3 |
| `backend/migrations/versions/<rev>_olcum_tablolari.py` | Şemanın DDL'i | 3 |
| `backend/app/sim/csv_reader.py` | CSV → dataclass. DB/zaman/döngü bilmez | 4, 5 |
| `backend/tests/test_csv_reader.py` | Ayrıştırıcı testleri | 4, 5 |
| `backend/app/sim/replay.py` | Yazma + budama + döngü + CLI | 6 |
| `backend/tests/test_replay.py` | Yazıcı testleri | 6 |
| `backend/app/measurements.py` | Teşhis ucu sorgusu + cevap modelleri | 7 |
| `backend/app/main.py` | Yolu bağlar (yalnız 8 satır) | 7 |
| `backend/tests/test_api.py` | Uç testleri | 7 |
| `basla.bat` | `csv` modu | 8 |
| `CLAUDE.md` | Yeni modun belgesi | 8 |

---

## Task 1: Veriyi depoya indir

`verı.zip` adındaki noktasız `ı`, `.bat`/PowerShell/Python yollarında kodlama tuzağıdır. CSV'ler ASCII adlı bir klasöre açılır, zip silinir.

**Files:**
- Create: `veri/10660/` (131 CSV), `veri/10665/` (131 CSV)
- Delete: `verı.zip`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: —
- Produces: `veri/<ünite no>/<dosya adı>.csv` düzeni. Ünite numarası **yalnızca klasör adında** vardır, dosyanın içinde yoktur. Sonraki görevler bu düzene dayanır.

- [ ] **Step 1: Zip'i `veri/` altına aç**

```powershell
Add-Type -AssemblyName System.IO.Compression.FileSystem
$zipYolu = (Get-ChildItem -Filter *.zip | Select-Object -First 1).FullName
$zip = [System.IO.Compression.ZipFile]::OpenRead($zipYolu)
foreach ($e in $zip.Entries) {
  if ($e.Length -eq 0) { continue }          # klasor girdileri
  $parca = $e.FullName -split '/'            # veri_son / 10660 / dosya.csv
  $hedef = Join-Path (Get-Location) ("veri\" + $parca[1] + "\" + $parca[2])
  New-Item -ItemType Directory -Force (Split-Path $hedef) | Out-Null
  [System.IO.Compression.ZipFileExtensions]::ExtractToFile($e, $hedef, $true)
}
$zip.Dispose()
```

Zip'in adı `Get-ChildItem` ile bulunur, elle yazılmaz — `ı` karakterini komut satırından geçirmemek içindir.

- [ ] **Step 2: Sayıları doğrula**

```powershell
"toplam : " + (Get-ChildItem veri -Recurse -Filter *.csv).Count
"10660  : " + (Get-ChildItem veri\10660 -Filter *.csv).Count
"10665  : " + (Get-ChildItem veri\10665 -Filter *.csv).Count
```

Beklenen: `262`, `131`, `131`. Tutmuyorsa devam etme.

- [ ] **Step 3: Zip'i sil**

```powershell
Get-ChildItem -Filter *.zip | Remove-Item
```

- [ ] **Step 4: `.gitignore`'a veritabanı dosyalarını ekle**

`.gitignore` dosyasının **en sonuna** (mevcut `frontend/bunfig.toml` satırından sonra) ekle:

```gitignore

# Yerel olcum veritabani (CSV sim yazar). CSV'ler versiyonlanir, DB versiyonlanmaz.
veri/*.db
veri/*.db-wal
veri/*.db-shm
```

- [ ] **Step 5: Doğrula ve commit**

```powershell
git add -A
git status --short | Select-Object -First 5
git commit -m "veri/ klasoru: 262 olcum CSV'si depoya alindi, veri.zip kaldirildi"
```

`git status` çıktısında `verı.zip` silinmiş, `veri/` altında 262 yeni dosya görünmeli.

---

## Task 2: Ayarlar, veritabanı motoru ve test yalıtımı

Bugün `settings.database_url` boş varsayılana sahiptir ve `db.py` hiçbir yerden içe aktarılmadığı için bu fark edilmez. Teşhis ucu gelince `db.py` içe aktarılacak; boş URL `create_engine`'i patlatır. Ayrıca SQLite'in iki varsayılanı bu şemada yanlış sonuç verir.

**Files:**
- Modify: `backend/app/config.py`
- Modify: `backend/app/db.py:1-18`
- Modify: `backend/tests/conftest.py`
- Test: `backend/tests/test_db.py` (yeni)

**Interfaces:**
- Consumes: Task 1'in `veri/` düzeni.
- Produces:
  - `settings.db_url() -> str`
  - `settings.csv_dir() -> Path`
  - `settings.sim_period_ms: int`, `settings.sim_retention_minutes: int`
  - `app.db.engine`, `app.db.SessionLocal`, `app.db.get_db()` (var olanlar korunur)

- [ ] **Step 1: Testleri yaz**

`backend/tests/test_db.py` (yeni dosya):

```python
"""DB URL cozumu ve SQLite PRAGMA'lari."""

from sqlalchemy import text

from app.config import Settings
from app.db import engine


def test_db_url_bos_env_ile_sqlite_dosyasina_duser():
    """Sifir kurulumla calismak icin varsayilan yerel bir dosyadir."""
    s = Settings(database_url="")
    url = s.db_url()
    assert url.startswith("sqlite+pysqlite:///")
    assert url.endswith("/veri/mazak.db")


def test_db_url_env_verilirse_o_kazanir():
    """.env'deki DATABASE_URL varsayilani EZER (.env.example'daki niyet)."""
    s = Settings(database_url="postgresql+psycopg://kul@sunucu/alp")
    assert s.db_url() == "postgresql+psycopg://kul@sunucu/alp"


def test_csv_dizini_varsayilani_depo_kokundeki_veri():
    s = Settings(sim_csv_dir="")
    assert s.csv_dir().name == "veri"
    assert (s.csv_dir() / "10660").is_dir()


def test_sqlite_pragmalari_acik():
    """Ikisi de SESSIZ hata kaynagidir; kapali olurlarsa hicbir sey patlamaz.

    foreign_keys kapaliysa budama cocuk satirlarini yetim birakir; WAL yoksa
    sim yazarken teshis ucu 'database is locked' ile duser.
    """
    with engine.connect() as c:
        assert c.execute(text("PRAGMA foreign_keys")).scalar() == 1
        assert c.execute(text("PRAGMA journal_mode")).scalar() == "wal"
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

```powershell
cd backend
uv run pytest tests/test_db.py -v
```

Beklenen: FAIL — `AttributeError: 'Settings' object has no attribute 'db_url'`.

- [ ] **Step 3: `config.py`'yi genişlet**

Dosyanın başına, `from pydantic_settings import ...` satırından sonra:

```python
from pathlib import Path

# Depo koku: <depo>/backend/app/config.py -> parents[2] = <depo>
_DEPO_KOKU = Path(__file__).resolve().parents[2]
```

`Settings` sınıfının içine, `feature_names()` metodundan **önce**:

```python
    # --- CSV tekrar-oynatma simulatoru (app/sim/replay.py) ---
    #
    # Bu simulator promos3_sim.exe DEGILDIR: o TEL bicimini uretir (UDP 1789),
    # bu ise OLCUM SATIRI uretir (CSV -> SQLite). Ikisi birbirine dokunmaz.

    # CSV kokü; bos ise depodaki veri/ kullanilir.
    sim_csv_dir: str = ""
    # Anlar arasi bekleme. 131 an x 1000 ms = tam tur ~2 dk 11 sn.
    sim_period_ms: int = 1000
    # Budama esigi. Sinirsiz buyuyen bir dosya, geceyi acik geciren bir
    # kosumda gigabaytlara cikar (1 sn periyotta saniyede 18 satir duser).
    sim_retention_minutes: int = 60
```

Aynı sınıfın sonuna, `feature_names()`'ten **sonra**:

```python
    def db_url(self) -> str:
        """Etkin veritabani URL'i.

        .env'de DATABASE_URL verilmisse O kazanir (.env.example'daki niyet);
        verilmemisse yerel SQLite dosyasina dusulur. Boylece kurulumsuz bir
        makinede .bat cift tiklanabilir kalir.

        Yol MUTLAKTIR: sim backend/ icinden, testler baska bir dizinden kosar
        - goreli bir yol iki AYRI dosya acar ve "yazdim ama gorunmuyor"
        seklinde ortaya cikardi.
        """
        if self.database_url:
            return self.database_url
        return f"sqlite+pysqlite:///{(_DEPO_KOKU / 'veri' / 'mazak.db').as_posix()}"

    def csv_dir(self) -> Path:
        """CSV kokü (icinde unite numarali klasorler)."""
        return Path(self.sim_csv_dir) if self.sim_csv_dir else _DEPO_KOKU / "veri"
```

- [ ] **Step 4: `db.py`'yi yeniden yaz**

`backend/app/db.py` tamamı:

```python
from collections.abc import Generator

from sqlalchemy import create_engine, event
from sqlalchemy.orm import Session, sessionmaker

from app.config import settings

_URL = settings.db_url()
_SQLITE = _URL.startswith("sqlite")

# check_same_thread: SQLite baglantisi varsayilan olarak kendi thread'ine
# kilitlidir; FastAPI istekleri havuzdan farkli thread'lerde alir.
engine = create_engine(
    _URL,
    pool_pre_ping=True,
    connect_args={"check_same_thread": False} if _SQLITE else {},
)


@event.listens_for(engine, "connect")
def _sqlite_pragmalari(dbapi_conn, _kayit) -> None:
    """SQLite'in iki varsayilani bu semada YANLIS sonuc verir - sessizce.

    - foreign_keys VARSAYILAN OLARAK KAPALIDIR. Acilmazsa ON DELETE CASCADE
      hic calismaz: budama olcum satirini siler, ozellik ve limit satirlari
      yetim kalir. Hicbir hata verilmez, dosya sessizce buyur.
    - journal_mode=WAL: sim YAZARKEN teshis ucu OKUYABILSIN. Iki AYRI surec
      ayni dosyayi acar; varsayilan journal'da yazan okuru kilitler ve
      /api/measurements "database is locked" ile duser.
    """
    if not _SQLITE:
        return
    imlec = dbapi_conn.cursor()
    imlec.execute("PRAGMA foreign_keys=ON")
    imlec.execute("PRAGMA journal_mode=WAL")
    imlec.close()


SessionLocal = sessionmaker(bind=engine, autoflush=False, autocommit=False)


def get_db() -> Generator[Session, None, None]:
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

- [ ] **Step 5: `conftest.py`'yi test DB'sine sabitle**

`backend/tests/conftest.py` tamamı:

```python
# Test ortami: app.config import edilmeden ONCE ayarlanmali.
# PROMOS3_ENABLED=false -> lifespan giris adaptorunu baslatmaz, yani testler
# 1789'u dinlemeye kalkmaz (ve gercekten kosan bir gateway/simulator ile port
# icin yarismaz).
import os
import tempfile
from pathlib import Path

os.environ["PROMOS3_ENABLED"] = "false"

# Testler ASLA veri/mazak.db'ye yazmaz: URL gecici bir dosyaya sabitlenir.
# Bu da app.config'ten ONCE olmali - Settings ortami ice aktarma aninda okur.
_TEST_DB = Path(tempfile.mkdtemp(prefix="mazak-test-")) / "test.db"
os.environ["DATABASE_URL"] = f"sqlite+pysqlite:///{_TEST_DB.as_posix()}"

import pytest


@pytest.fixture
def anyio_backend() -> str:
    # Async testler anyio isaretiyle kosar; tek backend: asyncio (trio kurulu degil).
    return "asyncio"
```

- [ ] **Step 6: Testleri koş**

```powershell
cd backend
uv run pytest tests/test_db.py -v
uv run pytest -q
```

Beklenen: `test_db.py` 4/4 PASS; toplam **64 passed** (60 eski + 4 yeni).

- [ ] **Step 7: Commit**

```powershell
git add backend/app/config.py backend/app/db.py backend/tests/conftest.py backend/tests/test_db.py
git commit -m "DB motoru: SQLite varsayilani, foreign_keys ve WAL pragmalari"
```

---

## Task 3: Şema — modeller ve migration

Projedeki ilk gerçek tablolar. `migrations/env.py` bugün `target_metadata = None` ve yorumunda tam bu adımı tarif ediyor.

**Files:**
- Create: `backend/app/models.py`
- Create: `backend/migrations/versions/<rev>_olcum_tablolari.py`
- Modify: `backend/migrations/env.py:17-20`
- Modify: `backend/tests/conftest.py` (`db_bos` fixture'ı eklenir)

**Interfaces:**
- Consumes: `app.db.engine` (Task 2).
- Produces:
  - `app.models.Base` — `DeclarativeBase`
  - `app.models.Measurement` — alanlar: `id, recorded_at, source_time, unit_no, channel_nr, tool_nr, program_nr, cut_nr, workpiece, alarm, alarm_limit, source_file, features, limits`
  - `app.models.MeasurementFeature` — `id, measurement_id, slot, name, value, work_value`
  - `app.models.MeasurementLimit` — `id, measurement_id, limit_nr, level, lim_type, feature_nr`
  - `db_bos` pytest fixture — her testte boş ve taze tablolar

- [ ] **Step 1: `models.py`'yi yaz**

`backend/app/models.py` (yeni dosya):

```python
"""Olcum tablolari — CSV tekrar-oynatma simulatorunun yazdigi sema.

Kolon adlari domain.py ile AYNI sozcukleri kullanir (unit_no, channel_nr,
feature_nr, lim_type, level), boylece CONTEXT.md'deki "Kod/kaynak"
karsiliklari tek kalir. domain.py'nin KENDISINE dokunulmaz: o dosya
frontend types.ts ile birebir sozlesmedir.

Sekil UZUN (long) formdur, GENIS degil: iki izleme unitesinin ozellik adlari
FARKLIDIR (10660: SPINDEL / X AXIS / Y AXIS / Z AXIS -- 10665: VIBRATION /
M131 DEBI / M131BASINC / M08 DEBI), dolayisiyla "her ozellige bir kolon" tek
tabloda toplanamaz.
"""

from datetime import datetime

from sqlalchemy import DateTime, Float, ForeignKey, Index, Integer, String
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column, relationship


class Base(DeclarativeBase):
    """Tum olcum tablolarinin ortak tabani (migrations/env.py buna baglanir)."""


class Measurement(Base):
    """Bir izleme unitesinden bir an — CSV dosyasi basina tek satir."""

    __tablename__ = "measurement"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    # Sim'in yazdigi an — UTC, naive. "Taze veri" olcusu budur.
    recorded_at: Mapped[datetime] = mapped_column(DateTime, index=True)
    # CSV'nin KENDI zamani (Start date) — yerel, naive. Kaynak kaybolmasin.
    source_time: Mapped[datetime] = mapped_column(DateTime)
    # 10660 / 10665. Dosyanin ICINDE yoktur; klasor adindan gelir.
    unit_no: Mapped[int] = mapped_column(Integer, index=True)
    channel_nr: Mapped[int] = mapped_column(Integer)
    tool_nr: Mapped[int] = mapped_column(Integer)
    program_nr: Mapped[int] = mapped_column(Integer)
    cut_nr: Mapped[int] = mapped_column(Integer)
    workpiece: Mapped[str | None] = mapped_column(String(64))
    alarm: Mapped[int | None] = mapped_column(Integer)
    alarm_limit: Mapped[int | None] = mapped_column(Integer)
    # Hangi dosyadan geldigi — bir deger tuhaf gorundugunde tek izlenebilirlik.
    source_file: Mapped[str] = mapped_column(String(128))

    # passive_deletes: silmeyi DB'ye birak. ORM'in cocuklari tek tek yukleyip
    # silmesi 65 bin satirlik budamada kabul edilemez.
    features: Mapped[list["MeasurementFeature"]] = relationship(
        back_populates="measurement",
        cascade="all, delete-orphan",
        passive_deletes=True,
        order_by="MeasurementFeature.slot",
    )
    limits: Mapped[list["MeasurementLimit"]] = relationship(
        back_populates="measurement",
        cascade="all, delete-orphan",
        passive_deletes=True,
        order_by="MeasurementLimit.limit_nr",
    )

    # Teshis ucunun TEK sorgu sekli: uniteye gore suz, zamana gore sirala.
    __table_args__ = (Index("ix_measurement_unit_time", "unit_no", "recorded_at"),)


class MeasurementFeature(Base):
    """Ozellik (CONTEXT: Ozellik / feature) — an basina 4 satir."""

    __tablename__ = "measurement_feature"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    measurement_id: Mapped[int] = mapped_column(
        ForeignKey("measurement.id", ondelete="CASCADE"), index=True
    )
    slot: Mapped[int] = mapped_column(Integer)
    # Ad kuruluma ozeldir, koda gomulmez: CSV kolon satirindan okunur.
    name: Mapped[str] = mapped_column(String(64))
    # Bos hucre None kalir, 0 OLMAZ: "sensor 0 olctu" ile "sensor yok" ayni
    # sey degildir (10665'te VIBRATION 131/131 bostur).
    value: Mapped[float | None] = mapped_column(Float)
    work_value: Mapped[float | None] = mapped_column(Float)

    measurement: Mapped[Measurement] = relationship(back_populates="features")


class MeasurementLimit(Base):
    """Limit (CONTEXT: Limit) — an basina 0..8 satir; bos yuva satir uretmez."""

    __tablename__ = "measurement_limit"

    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    measurement_id: Mapped[int] = mapped_column(
        ForeignKey("measurement.id", ondelete="CASCADE"), index=True
    )
    limit_nr: Mapped[int] = mapped_column(Integer)
    level: Mapped[float] = mapped_column(Float)
    lim_type: Mapped[int | None] = mapped_column(Integer)
    # Hangi ozellik yuvasina ait (CSV "Limit feature").
    feature_nr: Mapped[int | None] = mapped_column(Integer)

    measurement: Mapped[Measurement] = relationship(back_populates="limits")
```

- [ ] **Step 2: `migrations/env.py`'yi bağla**

`backend/migrations/env.py:17-20` şu satırları:

```python
# Model metadata'si. Projeye tablo eklendikce buraya baglanir:
#   from app.models import Base
#   target_metadata = Base.metadata
target_metadata = None
```

şununla değiştir:

```python
# Model metadata'si.
from app.models import Base

target_metadata = Base.metadata
```

- [ ] **Step 3: Boş revizyon üret**

```powershell
cd backend
uv run alembic revision -m "olcum tablolari"
```

Komut ürettiği dosyanın yolunu yazar. Sonraki adım o dosyayı doldurur. **Üretilen `revision` ve `down_revision` satırlarına dokunma** — `down_revision` `'0e4a34448ddb'` olmalıdır.

- [ ] **Step 4: Revizyonun gövdesini yaz**

Üretilen dosyadaki `upgrade()` ve `downgrade()` gövdelerini şununla değiştir (dosyanın üst kısmındaki `revision`/`down_revision` satırlarına **dokunma**):

```python
def upgrade() -> None:
    """Olcum tablolari (CSV tekrar-oynatma simulatorunun hedefi)."""
    op.create_table(
        "measurement",
        sa.Column("id", sa.Integer(), nullable=False),
        sa.Column("recorded_at", sa.DateTime(), nullable=False),
        sa.Column("source_time", sa.DateTime(), nullable=False),
        sa.Column("unit_no", sa.Integer(), nullable=False),
        sa.Column("channel_nr", sa.Integer(), nullable=False),
        sa.Column("tool_nr", sa.Integer(), nullable=False),
        sa.Column("program_nr", sa.Integer(), nullable=False),
        sa.Column("cut_nr", sa.Integer(), nullable=False),
        sa.Column("workpiece", sa.String(length=64), nullable=True),
        sa.Column("alarm", sa.Integer(), nullable=True),
        sa.Column("alarm_limit", sa.Integer(), nullable=True),
        sa.Column("source_file", sa.String(length=128), nullable=False),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_index("ix_measurement_recorded_at", "measurement", ["recorded_at"])
    op.create_index("ix_measurement_unit_no", "measurement", ["unit_no"])
    op.create_index("ix_measurement_unit_time", "measurement", ["unit_no", "recorded_at"])

    op.create_table(
        "measurement_feature",
        sa.Column("id", sa.Integer(), nullable=False),
        sa.Column("measurement_id", sa.Integer(), nullable=False),
        sa.Column("slot", sa.Integer(), nullable=False),
        sa.Column("name", sa.String(length=64), nullable=False),
        sa.Column("value", sa.Float(), nullable=True),
        sa.Column("work_value", sa.Float(), nullable=True),
        sa.ForeignKeyConstraint(["measurement_id"], ["measurement.id"], ondelete="CASCADE"),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_index(
        "ix_measurement_feature_measurement_id", "measurement_feature", ["measurement_id"]
    )

    op.create_table(
        "measurement_limit",
        sa.Column("id", sa.Integer(), nullable=False),
        sa.Column("measurement_id", sa.Integer(), nullable=False),
        sa.Column("limit_nr", sa.Integer(), nullable=False),
        sa.Column("level", sa.Float(), nullable=False),
        sa.Column("lim_type", sa.Integer(), nullable=True),
        sa.Column("feature_nr", sa.Integer(), nullable=True),
        sa.ForeignKeyConstraint(["measurement_id"], ["measurement.id"], ondelete="CASCADE"),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_index(
        "ix_measurement_limit_measurement_id", "measurement_limit", ["measurement_id"]
    )


def downgrade() -> None:
    op.drop_index("ix_measurement_limit_measurement_id", table_name="measurement_limit")
    op.drop_table("measurement_limit")
    op.drop_index("ix_measurement_feature_measurement_id", table_name="measurement_feature")
    op.drop_table("measurement_feature")
    op.drop_index("ix_measurement_unit_time", table_name="measurement")
    op.drop_index("ix_measurement_unit_no", table_name="measurement")
    op.drop_index("ix_measurement_recorded_at", table_name="measurement")
    op.drop_table("measurement")
```

- [ ] **Step 5: `conftest.py`'ye `db_bos` fixture'ı ekle**

`backend/tests/conftest.py` içinde `import pytest` satırından sonra ekle:

```python
from app.db import engine
from app.models import Base


@pytest.fixture
def db_bos():
    """Her testte bos ve taze tablolar.

    Migration DEGIL metadata kullanilir: testin dogruladigi sey semanin
    SEKLIDIR, alembic'in kendisi degil. Migration ayri bir adimda kosulur.
    """
    Base.metadata.drop_all(engine)
    Base.metadata.create_all(engine)
    yield
    Base.metadata.drop_all(engine)
```

- [ ] **Step 6: Migration'ı gerçek bir dosyaya uygula ve doğrula**

```powershell
cd backend
$env:DATABASE_URL = "sqlite+pysqlite:///$($env:TEMP -replace '\\','/')/migrasyon-denemesi.db"
uv run alembic upgrade head
uv run python -c "import sqlite3,os; d=sqlite3.connect(os.environ['TEMP']+'/migrasyon-denemesi.db'); print(sorted(r[0] for r in d.execute(\"select name from sqlite_master where type='table'\")))"
uv run alembic downgrade base
Remove-Item "$env:TEMP\migrasyon-denemesi.db"
Remove-Item Env:\DATABASE_URL
```

Beklenen tablo listesi: `['alembic_version', 'measurement', 'measurement_feature', 'measurement_limit']`. `downgrade` hatasız dönmeli.

- [ ] **Step 7: Testleri koş**

```powershell
cd backend
uv run pytest -q
uv run ruff check . 2>&1 | Select-Object -Last 2
```

Beklenen: 64 passed; ruff hâlâ **2 hata** (yalnız `0e4a34448ddb`).

- [ ] **Step 8: Commit**

```powershell
git add backend/app/models.py backend/migrations backend/tests/conftest.py
git commit -m "Olcum semasi: measurement + measurement_feature + measurement_limit"
```

---

## Task 4: CSV ayrıştırıcı — tek dosya

Saf fonksiyon: `Path` → dataclass. DB bilmez, zaman bilmez, döngü bilmez.

**Files:**
- Create: `backend/app/sim/__init__.py`
- Create: `backend/app/sim/csv_reader.py`
- Test: `backend/tests/test_csv_reader.py`

**Interfaces:**
- Consumes: Task 1'in `veri/<ünite>/*.csv` düzeni.
- Produces:
  - `read_measurement(path: Path, unit_no: int) -> MeasurementRecord | None`
  - `@dataclass(frozen=True) FeatureValue(slot: int, name: str, value: float | None, work_value: float | None)`
  - `@dataclass(frozen=True) LimitValue(limit_nr: int, level: float, lim_type: int | None, feature_nr: int | None)`
  - `@dataclass(frozen=True) MeasurementRecord(unit_no, source_time, channel_nr, tool_nr, program_nr, cut_nr, workpiece, alarm, alarm_limit, source_file, features, limits)`
  - Sabitler: `SUTUN_SAYISI = 41`, `OZELLIK_YUVASI = 4`, `LIMIT_YUVASI = 8`

- [ ] **Step 1: Testleri yaz**

`backend/tests/test_csv_reader.py` (yeni dosya):

```python
"""CSV ayristirici testleri — GERCEK veri/ dosyalarina karsi kosar.

Uydurma fixture yerine gercek veriyi kullanmak bilincli bir tercihtir:
biçimi bu 262 dosya tanimlar, bizim varsayimimiz degil.
"""

from app.config import settings
from app.sim.csv_reader import read_measurement

VERI = settings.csv_dir()


def test_gercek_dosya_tum_alanlari():
    p = VERI / "10660" / "000_01_00011_000_260810_112302.csv"
    r = read_measurement(p, 10660)
    assert r is not None
    assert r.unit_no == 10660
    assert (r.program_nr, r.channel_nr, r.tool_nr, r.cut_nr) == (0, 1, 11, 0)
    assert r.source_time.isoformat() == "2026-08-10T11:23:02.192000"
    assert r.source_file == "000_01_00011_000_260810_112302.csv"
    assert [f.name for f in r.features] == ["SPINDEL", "X AXIS", "Y AXIS", "Z AXIS"]
    assert [f.slot for f in r.features] == [1, 2, 3, 4]
    assert [f.value for f in r.features] == [115.0, 89.0, 96.0, 96.0]
    assert [f.work_value for f in r.features] == [87.0, 90.0, 85.0, 91.0]


def test_bos_hucre_none_olur_sifir_olmaz():
    """Workpiece ID 262/262 bostur; "" ya da 0 degil None olmali."""
    p = VERI / "10660" / "000_01_00011_000_260810_112302.csv"
    r = read_measurement(p, 10660)
    assert r is not None
    assert r.workpiece is None
    assert r.alarm is None
    assert r.alarm_limit is None


def test_bagli_olmayan_sensor_none_kalir():
    """10665'te VIBRATION 131/131 bostur; 0 yazmak 'sensor 0 olctu' derdi."""
    p = VERI / "10665" / "000_01_00011_000_260810_112302.csv"
    r = read_measurement(p, 10665)
    assert r is not None
    assert [f.name for f in r.features] == [
        "VIBRATION",
        "M131 DEBI",
        "M131BASINC",
        "M08 DEBI",
    ]
    assert r.features[0].value is None
    assert r.features[0].work_value is None
    assert r.features[1].value == 214.0
    assert r.features[1].work_value == 100.0


def test_bos_limit_yuvasi_satir_uretmez():
    """Limit 5..8 262/262 bostur: 8 degil 4 limit gelmeli."""
    p = VERI / "10660" / "000_01_00011_000_260810_112302.csv"
    r = read_measurement(p, 10660)
    assert r is not None
    assert [lv.limit_nr for lv in r.limits] == [1, 2, 3, 4]
    assert [lv.level for lv in r.limits] == [110.0, 110.0, 151.0, 110.0]
    assert [lv.lim_type for lv in r.limits] == [1, 1, 1, 1]
    assert [lv.feature_nr for lv in r.limits] == [1, 2, 3, 4]


def test_alarmli_an_okunur():
    """00043 alarmli bir andir: Alarm=1, Alarm limit=2."""
    p = VERI / "10660" / "000_01_00043_000_260810_114320.csv"
    r = read_measurement(p, 10660)
    assert r is not None
    assert r.alarm == 1
    assert r.alarm_limit == 2


def test_tum_dosyalar_cozulur():
    """262/262 — bir tanesi bile atlanirsa sim o ani sessizce kaybederdi."""
    dosyalar = sorted(VERI.rglob("*.csv"))
    assert len(dosyalar) == 262
    assert all(read_measurement(p, int(p.parent.name)) is not None for p in dosyalar)


def test_cok_kisa_dosya_none_doner(tmp_path):
    bozuk = tmp_path / "kisa.csv"
    bozuk.write_text("Program ; 0\nsadece iki satir\n", encoding="utf-8")
    assert read_measurement(bozuk, 10660) is None


def test_yanlis_kolon_sayisi_none_doner(tmp_path):
    """41 degil 5 kolon: sessizce yanlis kolondan okumaktansa ani ATLA."""
    bozuk = tmp_path / "dar.csv"
    bozuk.write_text(
        "Program ; 0\nChannel ; 1\nTool ; 11\nCut ; 0\n"
        "Start date ; 2026-08-10 11:23:02.192\n"
        "End date ; 2026-08-10 11:23:02.192\n"
        "File version ; 1\n\n"
        "Time (s) ; Date ; Time ; A ; B\n"
        "0 ; 10.08.2026 ; 11:23:02 ; 1 ; 2\n",
        encoding="utf-8",
    )
    assert read_measurement(bozuk, 10660) is None


def test_bilinmeyen_surum_none_doner(tmp_path):
    """File version 2 farkli bir yerlesim demektir; tahmin etmeyiz."""
    kaynak = (VERI / "10660" / "000_01_00011_000_260810_112302.csv").read_text(
        encoding="utf-8"
    )
    bozuk = tmp_path / "surum2.csv"
    bozuk.write_text(kaynak.replace("File version ; 1", "File version ; 2"), encoding="utf-8")
    assert read_measurement(bozuk, 10660) is None
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

```powershell
cd backend
uv run pytest tests/test_csv_reader.py -q
```

Beklenen: FAIL — `ModuleNotFoundError: No module named 'app.sim'`.

- [ ] **Step 3: Paketi ve ayrıştırıcıyı yaz**

`backend/app/sim/__init__.py` (boş dosya):

```python
```

`backend/app/sim/csv_reader.py` (yeni dosya):

```python
"""Provis olcum CSV'si -> dataclass. DB bilmez, zaman bilmez, dongu bilmez.

Bicim (`veri/<unite>/{program}_{kanal}_{takim}_{cut}_{YYMMDD}_{HHMMSS}.csv`):

    0  Program ; 0
    1  Channel ; 1
    2  Tool ; 11
    3  Cut ; 0
    4  Start date ; 2026-08-10 11:23:02.192
    5  End date ; 2026-08-10 11:23:02.192
    6  File version ; 1
    7  (bos)
    8  Time (s) ; Date ; Time ; Workpiece ID ; SPINDEL ; Work SPINDEL ; ...
    9  0 ; 10.08.2026 ; 11:23:02 ;  ; 115 ; 87 ; ...

Veri satiri 41 kolondur:

    0      Time (s)                  <- ALINMAZ: tek satirlik dosyada hep 0
    1,2    Date, Time
    3      Workpiece ID
    4..11  4 x (ozellik degeri, Work degeri)
    12,13  Alarm, Alarm limit
    14..16 Teach-In, Setup, Rework   <- ALINMAZ: 262/262 bos
    17..40 8 x (Limit N, Limit type, Limit feature)

Ozellik ADLARI kolon satirindan okunur, koda GOMULMEZ: adlar kuruluma
ozeldir (config.promos3_feature_names'in ayni gerekcesi).

UNITE NUMARASI DOSYANIN ICINDE YOKTUR; klasor adindan gelir ve cagirandan
parametre olarak alinir.
"""

import logging
from dataclasses import dataclass
from datetime import datetime
from pathlib import Path

log = logging.getLogger(__name__)

# Veri satirindaki kolon sayisi (262/262 dogrulandi). Uymazsa dosya atlanir.
SUTUN_SAYISI = 41
OZELLIK_YUVASI = 4
LIMIT_YUVASI = 8

_ILK_OZELLIK = 4  # (deger, Work deger) ciftleri buradan baslar
_ILK_LIMIT = 17  # (Limit, tip, ozellik) ucluleri buradan baslar


@dataclass(frozen=True)
class FeatureValue:
    """Bir ozellik yuvasinin o andaki degeri."""

    slot: int
    name: str
    value: float | None
    work_value: float | None


@dataclass(frozen=True)
class LimitValue:
    """Bir limit yuvasi. Bos yuvalar hic uretilmez."""

    limit_nr: int
    level: float
    lim_type: int | None
    feature_nr: int | None


@dataclass(frozen=True)
class MeasurementRecord:
    """Bir uniteden bir an — tek CSV dosyasinin tamami."""

    unit_no: int
    source_time: datetime
    channel_nr: int
    tool_nr: int
    program_nr: int
    cut_nr: int
    workpiece: str | None
    alarm: int | None
    alarm_limit: int | None
    source_file: str
    features: list[FeatureValue]
    limits: list[LimitValue]


def _hucreler(satir: str) -> list[str]:
    """`;` ile bol, her hucreyi kirp. Degerler bosluklarla sarilidir."""
    return [p.strip() for p in satir.split(";")]


def _baslik_degeri(satir: str) -> str:
    """`Tool ; 11` -> `11`."""
    _, _, deger = satir.partition(";")
    return deger.strip()


def _tam_sayi(hucre: str) -> int | None:
    return int(hucre) if hucre else None


def _ondalik(hucre: str) -> float | None:
    return float(hucre) if hucre else None


def read_measurement(path: Path, unit_no: int) -> MeasurementRecord | None:
    """Tek CSV -> kayit. Bicim beklenene uymazsa None doner ve neden loglanir.

    Sessizce yanlis kolondan deger okumaktansa o ani HIC yazmamak yegdir:
    DB'ye girmis yanlis bir deger, kaynagindan artik ayirt edilemez.
    """
    try:
        satirlar = path.read_text(encoding="utf-8").splitlines()
    except OSError as exc:
        log.warning("CSV okunamadi: %s (%s)", path.name, exc)
        return None

    if len(satirlar) < 10:
        log.warning("CSV cok kisa (%d satir): %s", len(satirlar), path.name)
        return None

    surum = _baslik_degeri(satirlar[6])
    if surum != "1":
        # Baska bir surum baska bir yerlesim demektir; tahmin etmeyiz.
        log.warning("Bilinmeyen File version %r: %s", surum, path.name)
        return None

    basliklar = _hucreler(satirlar[8])
    degerler = _hucreler(satirlar[9])
    if len(basliklar) != SUTUN_SAYISI or len(degerler) != SUTUN_SAYISI:
        log.warning(
            "Beklenen %d kolon; baslik=%d veri=%d: %s",
            SUTUN_SAYISI,
            len(basliklar),
            len(degerler),
            path.name,
        )
        return None

    try:
        return _cozumle(path, unit_no, satirlar, basliklar, degerler)
    except ValueError as exc:
        log.warning("Sayi cozulemedi: %s (%s)", path.name, exc)
        return None


def _cozumle(
    path: Path,
    unit_no: int,
    satirlar: list[str],
    basliklar: list[str],
    degerler: list[str],
) -> MeasurementRecord | None:
    """Sayisal cozme. ValueError firlatabilir; cagiran yakalar."""
    # fromisoformat hem ".192" olan hem olmayan bicimi kabul eder.
    source_time = datetime.fromisoformat(_baslik_degeri(satirlar[4]))

    # Satirdaki Date/Time ile basligin Start date'i AYNI ani gostermeli
    # (262/262 boyle). Tutmuyorsa dosyanin iki yarisi farkli anlardandir.
    satir_ani = f"{degerler[1]} {degerler[2]}"
    if satir_ani != source_time.strftime("%d.%m.%Y %H:%M:%S"):
        log.warning("Baslik/satir zamani tutmadi (%s): %s", satir_ani, path.name)
        return None

    features: list[FeatureValue] = []
    for yuva in range(OZELLIK_YUVASI):
        i = _ILK_OZELLIK + yuva * 2
        ad = basliklar[i]
        # "Work <ad>" eslesmesi kolon hizasinin KANITIDIR. Tutmuyorsa
        # yerlesim varsaydigimiz gibi degildir ve dosya atlanir.
        if basliklar[i + 1] != f"Work {ad}":
            log.warning(
                "Kolon hizasi tutmadi (%r / %r): %s", ad, basliklar[i + 1], path.name
            )
            return None
        features.append(
            FeatureValue(
                slot=yuva + 1,
                name=ad,
                value=_ondalik(degerler[i]),
                work_value=_ondalik(degerler[i + 1]),
            )
        )

    limits: list[LimitValue] = []
    for nr in range(LIMIT_YUVASI):
        i = _ILK_LIMIT + nr * 3
        if not degerler[i]:
            continue  # bos yuva satir uretmez (bu veride 5..8 hep bos)
        limits.append(
            LimitValue(
                limit_nr=nr + 1,
                level=float(degerler[i]),
                lim_type=_tam_sayi(degerler[i + 1]),
                feature_nr=_tam_sayi(degerler[i + 2]),
            )
        )

    return MeasurementRecord(
        unit_no=unit_no,
        source_time=source_time,
        program_nr=int(_baslik_degeri(satirlar[0])),
        channel_nr=int(_baslik_degeri(satirlar[1])),
        tool_nr=int(_baslik_degeri(satirlar[2])),
        cut_nr=int(_baslik_degeri(satirlar[3])),
        workpiece=degerler[3] or None,
        alarm=_tam_sayi(degerler[12]),
        alarm_limit=_tam_sayi(degerler[13]),
        source_file=path.name,
        features=features,
        limits=limits,
    )
```

- [ ] **Step 4: Testleri koş**

```powershell
cd backend
uv run pytest tests/test_csv_reader.py -v
```

Beklenen: 9/9 PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/sim/__init__.py backend/app/sim/csv_reader.py backend/tests/test_csv_reader.py
git commit -m "CSV ayristirici: tek dosya -> MeasurementRecord"
```

---

## Task 5: An keşfi ve iki ünitenin eşleştirilmesi

Aynı dosya adı iki ünitede de bulunur; "an" bu addır.

**Files:**
- Modify: `backend/app/sim/csv_reader.py` (sonuna eklenir)
- Modify: `backend/tests/test_csv_reader.py` (sonuna eklenir)

**Interfaces:**
- Consumes: `read_measurement` (Task 4).
- Produces:
  - `@dataclass(frozen=True) Moment(name: str, files: list[tuple[int, Path]])` — `files` içindeki her çift `(unit_no, path)`
  - `read_moments(root: Path) -> list[Moment]` — zaman sırasına dizilmiş; `root` yoksa `FileNotFoundError`

- [ ] **Step 1: Testleri yaz**

Önce dosyanın başındaki import bloğunu şununla değiştir:

```python
import pytest

from app.config import settings
from app.sim.csv_reader import Moment, read_measurement, read_moments
```

Sonra dosyanın sonuna ekle:

```python
def test_anlar_iki_uniteyi_eslestirir():
    anlar = read_moments(VERI)
    assert len(anlar) == 131
    # 54 ve 99 IKI unitede de eksik, o yuzden her an iki dosya tasir.
    assert all(len(a.files) == 2 for a in anlar)
    assert {u for a in anlar for u, _ in a.files} == {10660, 10665}
    assert isinstance(anlar[0], Moment)


def test_anlar_zaman_sirasinda():
    """Dosya adi ...{YYMMDD}_{HHMMSS} ile bittigi icin alfabetik = zaman sirasi."""
    anlar = read_moments(VERI)
    assert anlar[0].name == "000_01_00011_000_260810_112302.csv"
    assert anlar[-1].name == "000_01_00143_000_260810_123647.csv"


def test_eksik_sira_numaralari_an_uretmez():
    """54 ve 99 iki unitede de yok; olmayan ani uydurmuyoruz."""
    adlar = {a.name for a in read_moments(VERI)}
    assert not any("_00054_" in n or "_00099_" in n for n in adlar)


def test_tek_uniteli_an_da_kabul_edilir(tmp_path):
    """Bir uniteden eksik olan an, DIGER unite icin yine yazilmali."""
    (tmp_path / "10660").mkdir()
    (tmp_path / "10665").mkdir()
    kaynak = VERI / "10660" / "000_01_00011_000_260810_112302.csv"
    (tmp_path / "10660" / kaynak.name).write_bytes(kaynak.read_bytes())
    anlar = read_moments(tmp_path)
    assert len(anlar) == 1
    assert anlar[0].files == [(10660, tmp_path / "10660" / kaynak.name)]


def test_kok_yoksa_acik_hata(tmp_path):
    with pytest.raises(FileNotFoundError):
        read_moments(tmp_path / "olmayan")


def test_unite_klasoru_yoksa_acik_hata(tmp_path):
    with pytest.raises(FileNotFoundError):
        read_moments(tmp_path)
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

```powershell
cd backend
uv run pytest tests/test_csv_reader.py -q
```

Beklenen: FAIL — `ImportError: cannot import name 'Moment'`.

- [ ] **Step 3: `read_moments`'i yaz**

`backend/app/sim/csv_reader.py` sonuna ekle:

```python
@dataclass(frozen=True)
class Moment:
    """Bir an: ayni dosya adini tasiyan (unite no, yol) ciftleri.

    Iki unitenin dosya adlari BIREBIR AYNIDIR, bu yuzden eslestirme anahtari
    dosya adidir. Bir uniteden eksik olan an DIGER unite icin yine uretilir:
    bu veride 54 ve 99 ikisinde de eksiktir, ama bu genel kural degildir.
    """

    name: str
    files: list[tuple[int, Path]]


def read_moments(root: Path) -> list[Moment]:
    """`veri/` altini tarar, anlari zaman sirasinda dondurur.

    Unite numarasi KLASOR ADIDIR; sayisal olmayan klasorler atlanir.
    Kok ya da unite klasoru yoksa FileNotFoundError yukselir - sim'in bos bir
    listeyle sessizce donmesi "calisiyor ama hicbir sey yazmiyor" demek olurdu.
    """
    if not root.is_dir():
        raise FileNotFoundError(f"CSV kok dizini yok: {root}")

    uniteler = [
        (int(c.name), c) for c in sorted(root.iterdir()) if c.is_dir() and c.name.isdigit()
    ]
    if not uniteler:
        raise FileNotFoundError(f"{root} altinda unite klasoru yok (orn. 10660)")

    ada_gore: dict[str, list[tuple[int, Path]]] = {}
    for unit_no, klasor in uniteler:
        for csv in klasor.glob("*.csv"):
            ada_gore.setdefault(csv.name, []).append((unit_no, csv))

    # Dosya adi ...{YYMMDD}_{HHMMSS}.csv ile bittigi ve takim numarasi zamanla
    # arttigi icin ALFABETIK sira zaman sirasidir; ayrica ayristirmaya gerek yok.
    return [Moment(name=ad, files=ada_gore[ad]) for ad in sorted(ada_gore)]
```

- [ ] **Step 4: Testleri koş**

```powershell
cd backend
uv run pytest tests/test_csv_reader.py -v
```

Beklenen: 15/15 PASS.

- [ ] **Step 5: Commit**

```powershell
git add backend/app/sim/csv_reader.py backend/tests/test_csv_reader.py
git commit -m "An kesfi: iki uniteyi dosya adina gore eslestir"
```

---

## Task 6: Tekrar oynatma — yazma, budama, döngü, CLI

**Files:**
- Create: `backend/app/sim/replay.py`
- Test: `backend/tests/test_replay.py`

**Interfaces:**
- Consumes: `read_moments`, `read_measurement`, `Moment`, `MeasurementRecord` (Task 4-5); `Measurement`, `MeasurementFeature`, `MeasurementLimit` (Task 3); `SessionLocal`, `engine` (Task 2).
- Produces:
  - `write_moment(session: Session, moment: Moment, now: datetime) -> int` — yazılan `Measurement` sayısı
  - `prune(session: Session, older_than: datetime) -> int` — silinen `Measurement` sayısı
  - `run(csv_dir: Path, period_ms: int, retention_min: int, once: bool) -> int` — çıkış kodu
  - `main(argv: list[str] | None = None) -> int`

- [ ] **Step 1: Testleri yaz**

`backend/tests/test_replay.py` (yeni dosya):

```python
"""Tekrar oynatma testleri. conftest DATABASE_URL'i gecici dosyaya sabitler."""

from datetime import datetime, timedelta

from sqlalchemy import func, select

from app.config import settings
from app.db import SessionLocal
from app.models import Measurement, MeasurementFeature, MeasurementLimit
from app.sim.csv_reader import read_moments
from app.sim.replay import prune, run, write_moment


def _sayilar(s) -> tuple[int, int, int]:
    return (
        s.scalar(select(func.count()).select_from(Measurement)),
        s.scalar(select(func.count()).select_from(MeasurementFeature)),
        s.scalar(select(func.count()).select_from(MeasurementLimit)),
    )


def test_bir_an_18_satir_yazar(db_bos):
    """2 olcum + 8 ozellik + 8 limit = 18."""
    anlar = read_moments(settings.csv_dir())
    with SessionLocal() as s:
        assert write_moment(s, anlar[0], datetime(2026, 8, 11, 9, 0, 0)) == 2
        assert _sayilar(s) == (2, 8, 8)


def test_yazilan_satir_alanlari(db_bos):
    anlar = read_moments(settings.csv_dir())
    simdi = datetime(2026, 8, 11, 9, 0, 0)
    with SessionLocal() as s:
        write_moment(s, anlar[0], simdi)
        m = s.scalars(select(Measurement).where(Measurement.unit_no == 10665)).one()
        # recorded_at SIMDI, source_time CSV'nin kendi zamani.
        assert m.recorded_at == simdi
        assert m.source_time == datetime(2026, 8, 10, 11, 23, 2, 192000)
        assert m.tool_nr == 11
        assert m.source_file == "000_01_00011_000_260810_112302.csv"
        assert [f.name for f in m.features] == [
            "VIBRATION",
            "M131 DEBI",
            "M131BASINC",
            "M08 DEBI",
        ]
        assert m.features[0].value is None
        assert [lv.limit_nr for lv in m.limits] == [1, 2, 3, 4]


def test_ayni_an_iki_kez_yazilabilir(db_bos):
    """Dongu basa donunce cakisma OLMAMALI: recorded_at farklidir."""
    anlar = read_moments(settings.csv_dir())
    with SessionLocal() as s:
        write_moment(s, anlar[0], datetime(2026, 8, 11, 9, 0, 0))
        write_moment(s, anlar[0], datetime(2026, 8, 11, 9, 0, 1))
        assert _sayilar(s) == (4, 16, 16)


def test_budama_cocuklari_da_siler(db_bos):
    """FK cascade SQLite'ta YALNIZ foreign_keys=ON ile calisir (db.py pragma).

    Pragma unutulursa bu test cocuk satirlarini yetim bulur ve DUSER — sessiz
    sizintiya karsi tek korumamiz budur.
    """
    anlar = read_moments(settings.csv_dir())
    eski = datetime(2026, 8, 11, 9, 0, 0)
    yeni = eski + timedelta(minutes=90)
    with SessionLocal() as s:
        write_moment(s, anlar[0], eski)
        write_moment(s, anlar[1], yeni)
        assert _sayilar(s) == (4, 16, 16)
        assert prune(s, older_than=eski + timedelta(minutes=60)) == 2
        assert _sayilar(s) == (2, 8, 8)


def test_tek_gecis_tum_anlari_yazar(db_bos):
    """--once: 131 an x 2 unite = 262 olcum, 1048 cocuk satir."""
    assert run(settings.csv_dir(), period_ms=0, retention_min=60, once=True) == 0
    with SessionLocal() as s:
        assert _sayilar(s) == (262, 262 * 4, 262 * 4)


def test_tablo_yoksa_acik_hata_kodu(db_bos):
    """Tablolar dusuruldugunde sim patlamaz, ne yapilacagini soyler."""
    from app.db import engine
    from app.models import Base

    Base.metadata.drop_all(engine)
    assert run(settings.csv_dir(), period_ms=0, retention_min=60, once=True) == 2
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

```powershell
cd backend
uv run pytest tests/test_replay.py -q
```

Beklenen: FAIL — `ModuleNotFoundError: No module named 'app.sim.replay'`.

- [ ] **Step 3: `replay.py`'yi yaz**

`backend/app/sim/replay.py` (yeni dosya):

```python
"""CSV tekrar-oynatma simulatoru — veri/ altindaki olcumleri DB'ye yazar.

    cd backend && uv run python -m app.sim.replay

promos3_sim.exe ILE KARISTIRILMAZ: o TEL bicimini uretir (UDP 1789 -> hub ->
SSE -> ekran), bu ise OLCUM SATIRI uretir (CSV -> SQLite). Iki yol birbirine
dokunmaz; bu sim sokete tek bayt yazmaz.

Zaman damgasi: recorded_at = SIMDI, source_time = CSV'nin kendi zamani.
Dongu basa donunce cakisma olmaz ve kaynak zaman da kaybolmaz.
"""

import argparse
import logging
import time
from datetime import UTC, datetime, timedelta
from pathlib import Path

from sqlalchemy import delete, inspect
from sqlalchemy.exc import OperationalError
from sqlalchemy.orm import Session

from app.config import settings
from app.db import SessionLocal, engine
from app.models import Measurement, MeasurementFeature, MeasurementLimit
from app.sim.csv_reader import MeasurementRecord, Moment, read_measurement, read_moments

log = logging.getLogger("app.sim.replay")

# "database is locked" gecicidir (baska surec yaziyor). Kac kez denenecegi.
KILIT_DENEMESI = 3


def _satira_cevir(r: MeasurementRecord, now: datetime) -> Measurement:
    return Measurement(
        recorded_at=now,
        source_time=r.source_time,
        unit_no=r.unit_no,
        channel_nr=r.channel_nr,
        tool_nr=r.tool_nr,
        program_nr=r.program_nr,
        cut_nr=r.cut_nr,
        workpiece=r.workpiece,
        alarm=r.alarm,
        alarm_limit=r.alarm_limit,
        source_file=r.source_file,
        features=[
            MeasurementFeature(
                slot=f.slot, name=f.name, value=f.value, work_value=f.work_value
            )
            for f in r.features
        ],
        limits=[
            MeasurementLimit(
                limit_nr=lv.limit_nr,
                level=lv.level,
                lim_type=lv.lim_type,
                feature_nr=lv.feature_nr,
            )
            for lv in r.limits
        ],
    )


def write_moment(session: Session, moment: Moment, now: datetime) -> int:
    """Bir ani yazar; yazilan Measurement sayisini doner (0..unite sayisi).

    Bozuk bir dosya TUM ani dusurmez: o unite atlanir, digeri yazilir.
    """
    yazilan = 0
    for unit_no, path in moment.files:
        kayit = read_measurement(path, unit_no)
        if kayit is None:
            continue
        session.add(_satira_cevir(kayit, now))
        yazilan += 1
    session.commit()
    return yazilan


def prune(session: Session, older_than: datetime) -> int:
    """recorded_at esikten eski satirlari siler; silinen olcum sayisini doner.

    Cocuklar DB'nin ON DELETE CASCADE'i ile gider (models.py'de
    passive_deletes=True). ORM'in 65 bin satiri tek tek yuklemesi kabul
    edilemez olurdu.
    """
    silinen = session.execute(
        delete(Measurement).where(Measurement.recorded_at < older_than)
    ).rowcount
    session.commit()
    return silinen


def _yaz_yeniden_deneyerek(session: Session, moment: Moment, now: datetime) -> int:
    """SQLite kilidi gecicidir: kisa geri cekilmeyle birkac kez dene."""
    for deneme in range(KILIT_DENEMESI):
        try:
            return write_moment(session, moment, now)
        except OperationalError as exc:
            session.rollback()
            if "locked" not in str(exc).lower() or deneme == KILIT_DENEMESI - 1:
                log.warning("Yazilamadi (%s): %s", moment.name, exc)
                return 0
            time.sleep(0.2 * (deneme + 1))
    return 0


def run(csv_dir: Path, period_ms: int, retention_min: int, once: bool) -> int:
    """Ana dongu. Cikis kodu: 0 basarili, 2 tablolar yok."""
    anlar = read_moments(csv_dir)
    uniteler = sorted({u for a in anlar for u, _ in a.files})

    # Sessiz kosan bir simulator bu depoda daha once pahaliya patladi
    # (basla.bat madde 6): ne yaptigini acikca yazar.
    log.info("CSV SIM  -  yazilan her satir CSV'DEN gelir, canli olcum DEGILDIR")
    log.info("  kaynak     : %s", csv_dir)
    log.info("  an sayisi  : %d   uniteler: %s", len(anlar), uniteler)
    log.info("  veritabani : %s", settings.db_url())
    log.info("  periyot    : %d ms  (tam tur ~%.0f sn)", period_ms, len(anlar) * period_ms / 1000)
    log.info("  saklama    : %d dk", retention_min)

    if not inspect(engine).has_table("measurement"):
        log.error("`measurement` tablosu yok. Once semayi uygulayin:")
        log.error("    cd backend && uv run alembic upgrade head")
        return 2

    tur = 0
    with SessionLocal() as session:
        while True:
            tur += 1
            yazilan = 0
            for moment in anlar:
                now = datetime.now(UTC).replace(tzinfo=None)
                yazilan += _yaz_yeniden_deneyerek(session, moment, now)
                if period_ms > 0:
                    time.sleep(period_ms / 1000)

            silinen = prune(
                session,
                older_than=datetime.now(UTC).replace(tzinfo=None)
                - timedelta(minutes=retention_min),
            )
            log.info("tur %d bitti: %d satir yazildi, %d eski satir budandi", tur, yazilan, silinen)
            if once:
                return 0


def main(argv: list[str] | None = None) -> int:
    logging.basicConfig(level=logging.INFO, format="%(levelname)-5s %(message)s")
    ayristirici = argparse.ArgumentParser(
        description="veri/ altindaki olcum CSV'lerini yerel veritabanina yazar"
    )
    ayristirici.add_argument("--csv-dir", type=Path, default=settings.csv_dir())
    ayristirici.add_argument("--period-ms", type=int, default=settings.sim_period_ms)
    ayristirici.add_argument("--retention-min", type=int, default=settings.sim_retention_minutes)
    ayristirici.add_argument(
        "--once", action="store_true", help="tek gecis yapip cikar (test/teshis)"
    )
    a = ayristirici.parse_args(argv)
    return run(a.csv_dir, a.period_ms, a.retention_min, a.once)


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Testleri koş**

```powershell
cd backend
uv run pytest tests/test_replay.py -v
```

Beklenen: 6/6 PASS. (`test_tek_gecis_tum_anlari_yazar` 262 dosya okur; birkaç saniye sürebilir.)

- [ ] **Step 5: Gerçek veritabanına tek geçiş yaparak elle doğrula**

```powershell
cd backend
uv run alembic upgrade head
uv run python -m app.sim.replay --once --period-ms 0
uv run python -c "import sqlite3; from app.config import settings; p=settings.db_url().split('///')[1]; d=sqlite3.connect(p); print({t: d.execute(f'select count(*) from {t}').fetchone()[0] for t in ('measurement','measurement_feature','measurement_limit')})"
```

Beklenen: `{'measurement': 262, 'measurement_feature': 1048, 'measurement_limit': 1048}` ve `veri/mazak.db` oluşmuş olmalı.

- [ ] **Step 6: Commit**

```powershell
git add backend/app/sim/replay.py backend/tests/test_replay.py
git commit -m "CSV sim: dongusel tekrar oynatma, budama ve CLI"
```

---

## Task 7: Teşhis ucu `/api/measurements`

Yazma yolunun kanıtı. Salt okunur; ekranda karşılığı yoktur.

**Files:**
- Create: `backend/app/measurements.py`
- Modify: `backend/app/main.py` (import bloğu + `/api/alarms`'tan sonra yeni uç)
- Modify: `backend/tests/test_api.py` (sonuna eklenir)

**Interfaces:**
- Consumes: `app.models.Measurement` (Task 3), `app.db.get_db` (Task 2), `app.domain.CamelModel` (mevcut).
- Produces:
  - `MAX_LIMIT = 500`
  - `_clamp_limit(limit: int) -> int`
  - `read_page(db: Session, unit: int | None, limit: int) -> MeasurementPage`
  - `MeasurementPage(total: int, measurements: list[MeasurementOut])`
  - `GET /api/measurements?unit=&limit=`

- [ ] **Step 1: Testleri yaz**

`backend/tests/test_api.py` sonuna ekle:

```python
def test_measurements_bos_db(db_bos):
    with TestClient(app) as client:
        assert client.get("/api/measurements").json() == {"total": 0, "measurements": []}


def test_measurements_unite_suzgeci_ve_ic_ice_alanlar(db_bos):
    from datetime import datetime

    from app.config import settings
    from app.db import SessionLocal
    from app.sim.csv_reader import read_moments
    from app.sim.replay import write_moment

    anlar = read_moments(settings.csv_dir())
    with SessionLocal() as s:
        write_moment(s, anlar[0], datetime(2026, 8, 11, 9, 0, 0))

    with TestClient(app) as client:
        govde = client.get("/api/measurements?unit=10665").json()
        # total SUZGECE uyan TUM satir sayisidir; iki unite yazildi, biri suzuldu.
        assert govde["total"] == 1
        m = govde["measurements"][0]
        # Tel bicimi camelCase (mevcut uclarla ayni).
        assert m["unitNo"] == 10665
        assert m["toolNr"] == 11
        assert m["sourceFile"] == "000_01_00011_000_260810_112302.csv"
        assert [f["name"] for f in m["features"]] == [
            "VIBRATION",
            "M131 DEBI",
            "M131BASINC",
            "M08 DEBI",
        ]
        # Bagli olmayan sensor null gelir, 0 gelmez.
        assert m["features"][0]["value"] is None
        assert m["features"][1]["workValue"] == 100.0
        assert [lv["limitNr"] for lv in m["limits"]] == [1, 2, 3, 4]


def test_measurements_en_yeni_basta(db_bos):
    from datetime import datetime

    from app.config import settings
    from app.db import SessionLocal
    from app.sim.csv_reader import read_moments
    from app.sim.replay import write_moment

    anlar = read_moments(settings.csv_dir())
    with SessionLocal() as s:
        write_moment(s, anlar[0], datetime(2026, 8, 11, 9, 0, 0))
        write_moment(s, anlar[1], datetime(2026, 8, 11, 9, 0, 1))

    with TestClient(app) as client:
        govde = client.get("/api/measurements?unit=10660&limit=2").json()
        assert govde["total"] == 2
        assert govde["measurements"][0]["toolNr"] == 12
        assert govde["measurements"][1]["toolNr"] == 11


def test_measurements_limit_kirpilir():
    """Ust sinir olmadan `limit=999999` butun tabloyu bellege alirdi."""
    from app.measurements import MAX_LIMIT, _clamp_limit

    assert _clamp_limit(999999) == MAX_LIMIT
    assert _clamp_limit(0) == 1
    assert _clamp_limit(-5) == 1
    assert _clamp_limit(50) == 50
```

- [ ] **Step 2: Testin başarısız olduğunu gör**

```powershell
cd backend
uv run pytest tests/test_api.py -q
```

Beklenen: FAIL — 404 (`/api/measurements` yok) ve `ModuleNotFoundError: No module named 'app.measurements'`.

- [ ] **Step 3: `measurements.py`'yi yaz**

`backend/app/measurements.py` (yeni dosya):

```python
"""Olcum teshis ucu — SALT OKUYUCU.

Bu uc, CSV sim'in DB'ye GERCEKTEN yazip yazmadigini gorunur kilar. Ekranda
karsiligi YOKTUR (frontend bu fazda degismedi); tarayicidan Vite'in vekili
uzerinden acilir:  http://<LANIP>:5173/api/measurements

domain.py'ye DOKUNULMAZ - o dosya frontend types.ts ile birebir sozlesmedir.
Buradaki modeller yalnizca bu ucun cevabini tarif eder; ortak olan tek sey
camelCase taban sinifidir.
"""

from datetime import datetime

from pydantic import ConfigDict
from sqlalchemy import func, select
from sqlalchemy.orm import Session, selectinload

from app.domain import CamelModel
from app.models import Measurement

# Tek istekte donebilecek en fazla satir.
MAX_LIMIT = 500


class _OrmModel(CamelModel):
    """CamelModel + ORM nesnesinden dogrudan dogrulama.

    from_attributes SINIFTA acilir, model_validate cagrisinda DEGIL: cagrideki
    bayrak yalnizca en ust seviyeye uygulanir ve ic ice `features`/`limits`
    listeleri "not a valid dict" ile duserdi.

    ConfigDict pydantic v2'de kalitimla BIRLESIR; CamelModel'in camelCase
    ureteci ve populate_by_name'i korunur.
    """

    model_config = ConfigDict(from_attributes=True)


class FeatureOut(_OrmModel):
    slot: int
    name: str
    value: float | None = None
    work_value: float | None = None


class LimitOut(_OrmModel):
    limit_nr: int
    level: float
    lim_type: int | None = None
    feature_nr: int | None = None


class MeasurementOut(_OrmModel):
    id: int
    # Sim'in yazdigi an (UTC) — tazeligi bu gosterir.
    recorded_at: datetime
    # CSV'nin kendi zamani — kaynagi bu gosterir.
    source_time: datetime
    unit_no: int
    channel_nr: int
    tool_nr: int
    program_nr: int
    cut_nr: int
    workpiece: str | None = None
    alarm: int | None = None
    alarm_limit: int | None = None
    source_file: str
    features: list[FeatureOut] = []
    limits: list[LimitOut] = []


class MeasurementPage(CamelModel):
    # Suzgece uyan TUM satir sayisi (limit'ten etkilenmez): "veri geliyor mu"
    # sorusunun cevabi budur.
    total: int
    measurements: list[MeasurementOut] = []


def _clamp_limit(limit: int) -> int:
    """1..MAX_LIMIT araligina kirpar.

    Ust sinir olmadan `limit=999999` butun tabloyu bellege alirdi; sim
    saniyede 18 satir yaziyor.
    """
    return max(1, min(limit, MAX_LIMIT))


def read_page(db: Session, unit: int | None, limit: int) -> MeasurementPage:
    """En yeni olcumler (istege bagli unite suzgeciyle)."""
    kosul = [Measurement.unit_no == unit] if unit is not None else []

    total = db.scalar(select(func.count()).select_from(Measurement).where(*kosul)) or 0
    satirlar = db.scalars(
        select(Measurement)
        .where(*kosul)
        # selectinload: N+1 sorguyu onler — 50 olcum icin 3 sorgu, 101 degil.
        .options(selectinload(Measurement.features), selectinload(Measurement.limits))
        .order_by(Measurement.recorded_at.desc(), Measurement.id.desc())
        .limit(_clamp_limit(limit))
    ).all()

    return MeasurementPage(
        total=total,
        measurements=[MeasurementOut.model_validate(s) for s in satirlar],
    )
```

- [ ] **Step 4: Ucu `main.py`'ye bağla**

`backend/app/main.py:6-13` import bloğuna ekle (mevcut satırların arasına, alfabetik sırayı koruyarak):

```python
from fastapi import Depends, FastAPI, HTTPException
from sqlalchemy.orm import Session

from app.adapters.promos3_udp import run_promos3_ingest
from app.config import settings
from app.db import get_db
from app.domain import Alarm, EventRow
from app.hub import LiveHub
from app.measurements import MeasurementPage, read_page
```

`/api/alarms` ucundan **sonra**, `# --- Frontend (production) ---` yorumundan **önce** ekle:

```python
@app.get("/api/measurements")
def measurements(
    unit: int | None = None,
    limit: int = 50,
    db: Session = Depends(get_db),
) -> MeasurementPage:
    """CSV sim'in yazdigi olcumler (en yeni basta) — SALT OKUYUCU teshis ucu.

    Canli akisla (/api/stream, /api/live) ILGISI YOKTUR: o yol Promos3
    telgrafindan beslenir ve bellekte durur. Bu uc DB'yi okur.
    """
    return read_page(db, unit, limit)
```

- [ ] **Step 5: Testleri koş**

```powershell
cd backend
uv run pytest -q
```

Beklenen: **89 passed** = 60 eski + 4 (`test_db`) + 15 (`test_csv_reader`) + 6 (`test_replay`) + 4 (`test_api` yeni). Sayı tutmazsa eskilerden düşen var demektir — devam etme.

- [ ] **Step 6: Ucu canlı gör**

```powershell
cd backend
Start-Process -NoNewWindow uv -ArgumentList 'run','uvicorn','app.main:app','--port','8001'
Start-Sleep -Seconds 3
curl.exe -s "http://127.0.0.1:8001/api/measurements?unit=10665&limit=1"
Get-Process uvicorn -ErrorAction SilentlyContinue | Stop-Process -Force
```

Beklenen: `total` alanı Task 6 Step 5'te yazılan 262'yi (ünite süzgeciyle 131) gösteren JSON.

- [ ] **Step 7: Commit**

```powershell
git add backend/app/measurements.py backend/app/main.py backend/tests/test_api.py
git commit -m "Teshis ucu: GET /api/measurements (salt okunur)"
```

---

## Task 8: `basla.bat` `csv` modu ve belge

**Files:**
- Modify: `basla.bat`
- Modify: `CLAUDE.md` (`## Calistirma` bölümü)

**Interfaces:**
- Consumes: `uv run alembic upgrade head`, `uv run python -m app.sim.replay` (Task 3, 6); `/api/measurements` (Task 7).
- Produces: `.\basla.bat csv` (alias `veri`), menüde `[3]`, `ALP CSV Sim` pencere başlığı.

- [ ] **Step 1: Başlık belgesine yeni modu ve gerekçesini ekle**

`basla.bat` başlığında, `.\basla.bat sim` bloğu ile `.\basla.bat dur` satırı arasına ekle:

```
rem      .\basla.bat csv        Backend + arayuz + CSV SIMULATORU
rem                             (backend\app\sim\replay.py). veri\ altindaki
rem                             262 olcum CSV'sini DONGUSEL olarak yerel
rem                             SQLite'a yazar. "veri" de kabul edilir.
rem
rem                             Bu SIM DEGILDIR: promos3_sim.exe TEL bicimini
rem                             uretir (UDP 1789 -> ekran), bu ise OLCUM
rem                             SATIRI uretir (CSV -> DB). Bu kipte
rem                             PROMOS3_ENABLED=false'tur, yani UDP dinleyicisi
rem                             HIC kurulmaz ve izleme ekranlari durustce bos
rem                             durur.
```

Ve "Bedeli odenmis ayrintilar" listesinin sonuna 11. madde:

```
rem  11) CSV kipinde ARAYUZ ACILIR ama uygulama koku ACILMAZ. Sebep teknik:
rem      backend 127.0.0.1'de kalir (madde 8) ve bu makinede Chrome loopback'i
rem      goremez; Vite'in vekili uzerinden http://<LANIP>:5173/api/measurements
rem      ise ACILIR. Uygulama kokunu acmak yanlis olurdu - bu fazda ekranda
rem      gosterilecek yeni bir sey YOK, kok "calisiyor" izlenimi verirdi.
rem
rem      Ayrica bu kip DATABASE_URL'i SET ETMEZ: backend\.env varsa oradaki
rem      deger (orn. PostgreSQL) kazanir, yoksa veri\mazak.db kullanilir.
rem      Madde 2'nin "bayragi acikca yaz" kurali KAYNAK bayragi icindir; DB
rem      hedefi kullanicinin .env'iyle bilincli olarak degistirilebilir olmali.
```

- [ ] **Step 2: Mod ayrıştırma ve menüyü genişlet**

`basla.bat:122-127` şu bloğu:

```
if /i "%MODE%"=="promos3" set "MODE=provis"
if /i "%MODE%"=="provis" goto :kaynak_secildi
if /i "%MODE%"=="sahte" set "MODE=sim"
if /i "%MODE%"=="sim" goto :kaynak_secildi
if "%MODE%"=="" goto :kaynak_sor
goto :bilinmeyen_kip
```

şununla değiştir:

```
if /i "%MODE%"=="promos3" set "MODE=provis"
if /i "%MODE%"=="provis" goto :kaynak_secildi
if /i "%MODE%"=="sahte" set "MODE=sim"
if /i "%MODE%"=="sim" goto :kaynak_secildi
if /i "%MODE%"=="veri" set "MODE=csv"
if /i "%MODE%"=="csv" goto :kaynak_secildi
if "%MODE%"=="" goto :kaynak_sor
goto :bilinmeyen_kip
```

`basla.bat:129-139` menü bloğunu şununla değiştir:

```
:kaynak_sor
echo.
echo Veri kaynagi secin:
echo.
echo   [1] PROVIS  - Prometec gateway'i, UDP 1789. GERCEK kaynak; cihaz
echo                 bagli degilse ekran bos durur - dogru davranis.
echo   [2] SIM     - promos3_sim.exe, sentetik Promos3 UDP (cihazsiz).
echo                 Kapaliysa betik kendisi baslatir.
echo   [3] CSV     - veri\ altindaki olcum CSV'leri yerel SQLite'a yazilir.
echo                 Canli ekran DEGIL; teshis ucu /api/measurements.
echo.
choice /C 123 /N /T 10 /D 1 /M "Secim [1,2,3] (10 sn icinde secilmezse PROVIS): "
rem errorlevel N "N ya da daha buyuk" demektir; artan sirada atamak dogru
rem sonucu verir (tersi her zaman en dusugu secerdi).
set "MODE=provis"
if errorlevel 2 set "MODE=sim"
if errorlevel 3 set "MODE=csv"
```

- [ ] **Step 3: Ortam değişkenlerini ve mod yönlendirmesini düzelt**

`basla.bat:156` satırından hemen sonra ekle:

```
if /i "%MODE%"=="csv" set "KAYNAK_AD=CSV SIM - veri klasoru, SQLite"
if /i "%MODE%"=="csv" set "PROMOS3_ENABLED=false"
```

`basla.bat:162-165` şu bloğu:

```
if /i not "%MODE%"=="provis" goto :sim_hazirlik
call :sahte_var
if not errorlevel 1 goto :sahte_acik
goto :port_kontrol
```

şununla değiştir (üç kip üç ayrı yola gider; eskiden "provis degilse sim" varsayimi vardi ve csv de sim_hazirlik'e duserdi):

```
if /i "%MODE%"=="csv" goto :csv_hazirlik
if /i not "%MODE%"=="provis" goto :sim_hazirlik
call :sahte_var
if not errorlevel 1 goto :sahte_acik
goto :port_kontrol
```

- [ ] **Step 4: `:csv_hazirlik` bloğunu ekle**

`basla.bat`'ta bugün `:sim_hazirlik` **doğrudan** `:port_kontrol` etiketine akıyor. Araya yeni bir etiket koymak, SIM kipini o etikete de düşürür — bu yüzden `:sim_hazirlik`'in sonuna açık bir `goto` eklenmesi zorunludur.

Şu bloğu (`basla.bat:179-185`):

```
call :sahte_var
if not errorlevel 1 (
    echo       Onceden kosan simulator bulundu, durduruluyor ^(madde 6^).
    call :sahte_oldur
)

:port_kontrol
```

şununla değiştir:

```
call :sahte_var
if not errorlevel 1 (
    echo       Onceden kosan simulator bulundu, durduruluyor ^(madde 6^).
    call :sahte_oldur
)
rem Bu "goto" GEREKLIDIR: altta artik baska bir etiket var ve SIM kipi ona
rem duserse olmayan bir alembic adimini kosardi. Eskiden :sim_hazirlik
rem dogrudan :port_kontrol'e akiyordu.
goto :port_kontrol

rem Buraya yalnizca CSV kipi duser. PROVIS'teki "simulator acik olmasin"
rem kilidi bunu KAPSAMAZ: o kilit UDP 1789 cakismasi icindir, CSV sim sokete
rem hic dokunmaz.
:csv_hazirlik
if not exist "veri\10660" goto :yok_veri
echo.
echo       Veritabani semasi guncelleniyor (alembic upgrade head)...
pushd backend
uv run alembic upgrade head
set "ALEMBIC_RC=%ERRORLEVEL%"
popd
if not "%ALEMBIC_RC%"=="0" goto :migration_hatasi

:port_kontrol
```

- [ ] **Step 5: Simülatör başlatma dallarını ayır**

`basla.bat:206-223` (`:hazir` bloğu) şunu:

```
:hazir
echo       backend hazir.
if /i not "%MODE%"=="sim" goto :arayuz_bekle

echo.
echo [3/3] SIMULATOR baslatiliyor...
```

şununla değiştir:

```
:hazir
echo       backend hazir.
if /i "%MODE%"=="sim" goto :sim_baslat
if /i "%MODE%"=="csv" goto :csv_baslat
goto :arayuz_bekle

:csv_baslat
echo.
echo [3/3] CSV SIMULATORU baslatiliyor...
echo       ####################################################################
echo       #  DIKKAT: DB'ye yazilan HER SATIR veri\ altindaki CSV'lerden      #
echo       #  gelir. Bu bir TEKRAR OYNATMADIR, canli olcum DEGILDIR.          #
echo       ####################################################################
start "ALP CSV Sim" /D "%CD%\backend" cmd /k uv run python -m app.sim.replay
goto :arayuz_bekle

:sim_baslat
echo.
echo [3/3] SIMULATOR baslatiliyor...
```

(`:sim_baslat` bloğunun geri kalanı — uyarı çerçevesi, madde 10 yorumu ve `start "ALP Simulator" ...` satırı — olduğu gibi kalır.)

- [ ] **Step 6: Banner ve durdurma**

`basla.bat:261-262` (kip satırları) şunu:

```
if /i "%MODE%"=="sim" echo   Kip: SIMULATOR - ekrandaki hicbir sayi olcum degildir.
if /i "%MODE%"=="provis" echo   Kip: PROVIS, gercek kaynak - cihaz bagli degilse ekran bos durur (dogru davranis).
```

şununla değiştir:

```
if /i "%MODE%"=="sim" echo   Kip: SIMULATOR - ekrandaki hicbir sayi olcum degildir.
if /i "%MODE%"=="provis" echo   Kip: PROVIS, gercek kaynak - cihaz bagli degilse ekran bos durur (dogru davranis).
if /i "%MODE%"=="csv" echo   Kip: CSV SIM - olcumler veri\ klasorunden okunup SQLite'a yazilir.
if /i "%MODE%"=="csv" echo        Izleme ekranlari BOS durur (UDP dinleyicisi bu kipte kurulmaz).
if /i "%MODE%"=="csv" echo        Yazilani gormek icin: http://%LANIP%:5173/api/measurements
```

`basla.bat:266` (`start "" http://%LANIP%:5173/`) satırını şununla değiştir:

```
rem Madde 11: CSV kipinde uygulama koku DEGIL teshis ucu acilir.
if /i "%MODE%"=="csv" (start "" http://%LANIP%:5173/api/measurements) else (start "" http://%LANIP%:5173/)
```

`basla.bat:274` (`taskkill ... ALP Simulator*`) satırından sonra ekle:

```
taskkill /FI "WINDOWTITLE eq ALP CSV Sim*" /T /F >nul 2>nul
```

- [ ] **Step 7: Hata etiketlerini ekle**

`:bilinmeyen_kip` mesajını güncelle:

```
:bilinmeyen_kip
echo HATA: bilinmeyen kip "%MODE%". Gecerli kipler: provis, sim, sahte, csv, veri, dur.
echo       Arguman vermezseniz betik kaynagi menuden sorar.
goto :son
```

`:yok_sim` bloğundan sonra iki yeni etiket ekle:

```
:yok_veri
echo HATA: veri\10660 klasoru yok (CSV kipi bunu okur).
echo       262 olcum CSV'si depoda veri\10660 ve veri\10665 altinda durur.
echo       Eksikse depoyu guncelleyin:  git pull
goto :son

:migration_hatasi
echo HATA: "alembic upgrade head" basarisiz (cikis kodu %ALEMBIC_RC%).
echo       Elle calistirip hatayi okuyun:
echo         cd backend
echo         uv run alembic upgrade head
goto :son
```

- [ ] **Step 8: CRLF satır sonlarını doğrula**

```powershell
$b = [System.IO.File]::ReadAllBytes("basla.bat")
$lf = 0; $crlf = 0
for ($i=0; $i -lt $b.Length; $i++) {
  if ($b[$i] -eq 10) { $lf++; if ($i -gt 0 -and $b[$i-1] -eq 13) { $crlf++ } }
}
"LF=$lf CRLF=$crlf"
```

Beklenen: `LF` ile `CRLF` **eşit**. Değilse dosyayı CRLF'e çevir:

```powershell
$m = Get-Content basla.bat -Raw
[System.IO.File]::WriteAllText((Resolve-Path basla.bat), ($m -replace "`r?`n", "`r`n"))
```

- [ ] **Step 9: `CLAUDE.md`'yi güncelle**

`## Calistirma` bölümündeki "Gunluk:" satırını şununla değiştir:

```markdown
Gunluk: `.\basla.bat` (kaynak menusu: PROVIS/SIM/CSV) / `.\basla.bat provis` /
`.\basla.bat sim` (eski adi `sahte`, hala kabul edilir) / `.\basla.bat csv`
(eski adi `veri`) / `.\basla.bat dur`

`csv` kipi ayri bir simulatordur: `veri\` altindaki 262 olcum CSV'sini
DONGUSEL olarak yerel SQLite'a (`veri\mazak.db`) yazar. `promos3_sim.exe` ILE
KARISTIRILMAZ — o TEL bicimini uretir (UDP 1789 -> ekran), bu OLCUM SATIRI
uretir (CSV -> DB). Bu kipte `PROMOS3_ENABLED=false`'tur, izleme ekranlari
durustce bos durur; yazilan veri yalnizca `/api/measurements` ucundan gorulur.
```

- [ ] **Step 10: Uçtan uca doğrula**

```powershell
.\basla.bat csv
```

Kontrol listesi:
1. `ALP Backend`, `ALP Frontend`, `ALP CSV Sim` pencereleri açılır.
2. `ALP CSV Sim` penceresinde `an sayisi : 131   uniteler: [10660, 10665]` yazar.
3. Tarayıcı `http://<LANIP>:5173/api/measurements` açar, JSON gelir.
4. Sayfayı yenile — `total` artmış olmalı.

Sonra:

```powershell
.\basla.bat dur
```

5. Üç pencere de kapanır; sayfa yenilendiğinde `total` artık artmaz.

- [ ] **Step 11: Son kontroller ve commit**

```powershell
cd backend
uv run pytest -q
uv run ruff check . 2>&1 | Select-Object -Last 2
cd ..\frontend
bun run lint
cd ..
git status --short
```

Beklenen: pytest hepsi geçer; ruff **2 hata** (değişmedi); `bun run lint` geçer; `git status` çıktısında `frontend/` altında **hiçbir** değişiklik yok.

```powershell
git add basla.bat CLAUDE.md
git commit -m "basla.bat: csv kipi (CSV sim + alembic + teshis ucu)"
```

---

## Kabul ölçütleri (spec §13)

| # | Ölçüt | Nerede doğrulanır |
|---|---|---|
| 1 | `.\basla.bat csv` üç pencere açar, `total` artar | Task 8 Step 10 |
| 2 | `veri/mazak.db` oluşur, döngü başa döner | Task 6 Step 5, Task 8 Step 10 |
| 3 | `.\basla.bat dur` CSV sim'i de kapatır | Task 8 Step 10 |
| 4 | `uv run pytest` tamamı geçer | Task 8 Step 11 |
| 5 | `bun run lint` geçer | Task 8 Step 11 |
| 6 | `frontend/` altında hiç değişiklik yok | Task 8 Step 11 |
